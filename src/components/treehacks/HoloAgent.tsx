/**
 * Shared mood vocabulary for the Builder Guide. The guide itself is rendered by
 * HoloKiosk (a rigged 3D human inside a glass cylinder). The panels set the mood
 * so the figure reacts: it glows green while it listens and shifts toward violet
 * while it scans.
 */
export type AgentMood = "idle" | "talking" | "scanning" | "happy";
